# cells m4 testplan

One acceptance check per claimed feature. Walkthrough commands are written by `/implement-m`.

| ID | Check |
|----|--------|
| CELL-003 | For each of INVERTER, NAND, NOR, AND, OR, XOR, TG, MUX2, LATCH, ADDER: an `.asy` in `lt-spice/` has `Prefix X`, `Value` (or `SpiceModel`) equal to that name, and `ModelFile` contains `yap_cells.lib` |
